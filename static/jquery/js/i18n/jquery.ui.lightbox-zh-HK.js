/* Chinese initialisation for the jQuery UI lightBox plugin. */
jQuery(function($){
	$.extend($.lightBox, {
		textBtnPrev:			'上一張',	// (string) the text of prev button
		textBtnNext:			'下一張',	// (string) the text of next button
		textBtnClose:			'關閉×'		// (string) the text of close button
	});
});
