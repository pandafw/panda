package panda.dao.entity;

/**
 * abstract column type
 * @author yf.frank.wang@gmail.com
 */
public enum ColType {
	CHAR, BOOLEAN, VARCHAR, TEXT, BINARY, TIMESTAMP, DATETIME, DATE, TIME, INT, FLOAT
}
