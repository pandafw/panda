/* Chinese initialisation for the jQuery UI lightBox plugin. */
jQuery(function($){
	$.extend($.lightbox, {
		textBtnPrev:			'上一张',	// (string) the text of prev button
		textBtnNext:			'下一张',	// (string) the text of next button
		textBtnClose:			'关闭×'		// (string) the text of close button
	});
});
